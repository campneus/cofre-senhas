<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Erro - Cofre Campneus' %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">
    <style>
        .error-bg {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        }
        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
    </style>
</head>
<body class="error-bg min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-md text-center">
        <!-- Ícone de Erro -->
        <div class="mb-8">
            <div class="bg-white p-6 rounded-full inline-block shadow-lg mb-4">
                <i class="fas fa-exclamation-triangle text-red-500 text-6xl"></i>
            </div>
        </div>

        <!-- Conteúdo do Erro -->
        <div class="glass-effect rounded-2xl shadow-2xl p-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-4">
                <%= title || 'Oops! Algo deu errado' %>
            </h1>
            
            <p class="text-gray-600 mb-6">
                <%= message || 'Ocorreu um erro inesperado. Por favor, tente novamente.' %>
            </p>

            <div class="space-y-4">
                <button 
                    onclick="history.back()" 
                    class="w-full bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-3 px-4 rounded-lg transition duration-200"
                >
                    <i class="fas fa-arrow-left mr-2"></i>
                    Voltar
                </button>
                
                <a 
                    href="/" 
                    class="block w-full bg-gray-500 hover:bg-gray-600 text-white font-semibold py-3 px-4 rounded-lg transition duration-200 text-decoration-none"
                >
                    <i class="fas fa-home mr-2"></i>
                    Ir para o Início
                </a>
            </div>
        </div>

        <!-- Footer -->
        <div class="mt-8">
            <p class="text-yellow-100 text-sm">
                Se o problema persistir, entre em contato com o suporte.
            </p>
        </div>
    </div>

    <script>
        // Auto-redirect para home após 10 segundos se for erro 404
        if (window.location.pathname.includes('404') || document.title.includes('404')) {
            let countdown = 10;
            const countdownElement = document.createElement('p');
            countdownElement.className = 'text-yellow-100 text-sm mt-4';
            countdownElement.innerHTML = `Redirecionando para a página inicial em <span id="countdown">${countdown}</span> segundos...`;
            document.body.appendChild(countdownElement);

            const timer = setInterval(() => {
                countdown--;
                document.getElementById('countdown').textContent = countdown;
                
                if (countdown <= 0) {
                    clearInterval(timer);
                    window.location.href = '/';
                }
            }, 1000);
        }
    </script>
</body>
</html>

